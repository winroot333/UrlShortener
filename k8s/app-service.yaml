apiVersion: v1
kind: Service
metadata:
  name: url-shortener-service
  namespace: url-shortener
  labels:
    app: url-shortener
    env: dev
    owner: Mikhail
spec:
  selector:
    app: url-shortener
  ports:
    - port: 8080
      targetPort: 8080
  type: NodePort
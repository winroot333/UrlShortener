spring:
  application.name: UrlShortener
  datasource:
    url: jdbc:postgresql://localhost:5432/url_shortener
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver

app:
  base-url: http://localhost:8080
  link:
    random-code-digits: 6